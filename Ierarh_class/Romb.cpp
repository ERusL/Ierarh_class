#include "Romb.h"

Romb::Romb() 
{
	std::string name = "Ромб";
}

Romb :: Romb (int _a, int _A, int _B)
	{
		a = b = c = d;
		A = C;
		B = D;
	}

Romb :: Romb(int, int, int)
	{
		a = b = c = d = 20;
		A = C = 40;
		B = D = 140;
	}
	 
		bool  Romb :: check() {
		check();
		if (a == c == b == d && A == C && B == D)
			return true;
		}
	 
		void Romb :: print_info()  {
		print_info();
		}
